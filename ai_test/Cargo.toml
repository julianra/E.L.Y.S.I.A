[package]
name = "ai_test"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
elysia_ai = { path = "../elysia_ai" }

# Voor logging (zelfde als kernel)
log = "0.4"
env_logger = "0.11"
